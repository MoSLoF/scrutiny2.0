# Compiled eBPF bytecode — regenerated by `make bpf` from bpf/*.c.
# Never commit compiled objects; the C source is the source of truth.
*.o

# Compiled Go binaries
/scrutiny
/scrutiny.exe
/scrutiny-arm64
/scrutiny-armv7
/dist/

# Runtime output — baselines, observations, logs
*.log
/baselines/*.json
/observations/*.json

# Go tooling
/vendor/
